# Backlog — SDK и генератор тегов

Проблемы продукта, не полигона. Полигон не чинит генератор и `vb.js`.

- Smart Tag из кабинета всегда содержит `id="visibla-click-area"`. Несколько копий тега на одной странице дают невалидный HTML и ломают клик.
- Публичный `GET /api/vast/{creative_id}.xml` пока не отдаётся. Полигон играет XML из вкладки кабинета.
- Если Kinescope не исполнит VPAID-unit `vb.js` (`apiFramework="VPAID"`), нужен плеер РФ, который вызывает `initAd`. Зафиксировать после первого прогона.
